{
  "extends": ["peerigon"],
  "env": {
    "node": true
  },
  "root": true,
  "rules": {
    // We have a lot of CommonJS modules, so this warning doesn't really help
    "import/unambiguous": "off"
  }
}